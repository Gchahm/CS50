1.  it's a library used to define sets of int types.
2.  they are used to define the size of the labels.
3.  1,4,4,2 bytes;
4.  BM
5.  file size and image size.
6.  if it's negative means that it reads from top to bottom having its origin in the top left corner
7.  biBitCount
8.  fopen returns null if it couldn't open the file errno is set to indicate the error
9.  so it reads the file pixel per pixel
10. 3
11. fseek is used to set the file position indicator 
12. seek_cur means that it'll skip the padding size from the current position indicator
